[package]
name = "local_smtp"
version = "0.1.0"
edition = "2024"

[dependencies]
samotop = "0.13.2"
async-std = "1.13.2"

[profile.minimal]
inherits = "release"
opt-level = "z"
lto = "thin"
codegen-units = 1
strip = "symbols"
panic = "abort"
incremental = false
